# Quick Setup Locally

> Install the Dependencies

```
cd daily-code
npm install
```

> Setup DB (Windows users must have git-bash installed to run the script)

```
cd packages/db
./setupDB.sh
```

> Run locally

```
cd ../..
npm run dev
```
